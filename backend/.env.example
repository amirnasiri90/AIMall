# ===========================================
# AIMall Backend — نمونهٔ متغیرهای محیطی
# این فایل را کپی کنید به .env و مقادیر را پر کنید.
# در production هرگز .env را در گیت commit نکنید.
# ===========================================

# دیتابیس — برای تفکیک آینده فقط این آدرس را عوض کنید (داده پاک نمی‌شود)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/aimall?schema=public

# امنیت
JWT_SECRET=یک-رشته-بلند-و-تصادفی-برای-امضای-JWT

# پورت بک‌اند
PORT=3001

# آدرس فرانت‌اند (برای CORS و لینک‌ها). در حالت سه سرور آدرس واقعی سایت را بگذارید.
FRONTEND_URL=http://localhost:3000

# OpenRouter (برای چت و مدل‌ها)
OPENROUTER_API_KEY=

# پرداخت زرین‌پال
ZARINPAL_MERCHANT_ID=
ZARINPAL_SANDBOX=true
# ZARINPAL_CALLBACK_URL=https://your-backend-domain.com/api/v1/billing/payment/verify

# پیامک (sms.ir)
# SMS_IR_API_KEY=
# SMS_IR_TEMPLATE_ID=984933

# پیامک جایگزین (sms-webservice.com)
# SMS_API_KEY=
# SMS_SENDER=

# صف کارها (اختیاری؛ در صورت خالی بودن از صف دیتابیس استفاده می‌شود)
# REDIS_URL=redis://localhost:6379

# برای رفرر در درخواست به OpenRouter و سرویس‌های خارجی
# BACKEND_URL=https://api.your-domain.com
