# ===========================================
# AIMall Frontend — نمونهٔ متغیرهای محیطی
# کپی به .env.local و مقادیر را پر کنید.
# ===========================================

# آدرس API بک‌اند:
# - در حالت یک سرور پشت Nginx با یک دامنه: خالی بگذارید تا از /api/v1 استفاده شود.
# - در حالت تفکیک سرور یا دسترسی مستقیم به API:
#   NEXT_PUBLIC_API_URL=https://api.your-domain.com/api/v1
NEXT_PUBLIC_API_URL=

# فقط برای build در سرور (اختیاری)
# NEXT_PUBLIC_BACKEND_PORT=3001
